#!/usr/bin/env python3

# pylint: disable=missing-docstring

import functools
import importlib.util
import os
import shlex
import shutil
import sys
from pathlib import Path


XDG_CONFIG_HOME = Path(
    os.environ.get("XDG_CONFIG_HOME", Path.home() / ".config"))
USER_CONFIG_DIR = XDG_CONFIG_HOME / "nixulate"
USER_CACHE_DIR = Path(
    os.environ.get("XDG_CACHE_HOME", Path.home() / ".cache")) / "nixulate"


def main():
    load_config()
    program = ["bash"] if len(sys.argv) == 1 else sys.argv[1:]
    bwrap = ["bwrap", *bwrap_options(), *program]
    command = f"exec {shlex.join(bwrap)}"
    os.execvp("nix-shell", [
        "nix-shell",
        "--packages", *nix_shell_packages(),
        "--run", command,
    ])


def load_config():
    # Alias so config's `import nixulate` finds us, not a second instance.
    sys.modules.setdefault("nixulate", sys.modules[__name__])
    project_dir = Path.cwd() / ".nixulate"
    # Later wins: project dir over global, config.local.py over config.py.
    paths = [
        USER_CONFIG_DIR / "config.py",
        project_dir / "config.py",
        project_dir / "config.local.py",
    ]
    for path in paths:
        if path.exists():
            exec_config(path)


def exec_config(path):
    sys.path.append(str(path.parent))
    spec = importlib.util.spec_from_file_location("nixulate_config", path)
    module = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(module)


def bwrap_options():
    cert = "/etc/ssl/certs/ca-certificates.crt"
    home = Path.home()
    cwd = Path.cwd()
    nix_conf = os.path.realpath("/etc/nix/nix.conf")
    dbus = f"/run/user/{os.getuid()}/bus"
    return [
        "--unshare-all",
        "--die-with-parent",
        "--share-net",
        "--ro-bind", "/etc/passwd", "/etc/passwd",
        "--ro-bind", "/etc/resolv.conf", "/etc/resolv.conf",
        "--ro-bind", cert, cert,
        "--proc", "/proc",
        "--dev", "/dev",
        "--dev-bind", "/dev/kvm", "/dev/kvm",
        "--tmpfs", "/tmp",
        "--setenv", "TMPDIR", "/tmp",
        "--ro-bind", "/nix/store", "/nix/store",
        "--bind", "/nix/var/nix/daemon-socket", "/nix/var/nix/daemon-socket",
        "--ro-bind", "/nix/var/nix/profiles", "/nix/var/nix/profiles",
        "--ro-bind", nix_conf, "/etc/nix/nix.conf",
        "--ro-bind", "/run/current-system/sw", "/run/current-system/sw",
        "--setenv", "NIX_REMOTE", "daemon",
        "--ro-bind", "/usr/bin/env", "/usr/bin/env",
        "--ro-bind", "/bin/sh", "/bin/sh",
        "--tmpfs", str(home),
        "--setenv", "HOME", str(home),
        "--setenv", "USER", os.environ["USER"],
        "--setenv", "PS1", r"[nixulate] \w \$ ",
        "--bind", dbus, dbus,
        "--setenv", "DBUS_SESSION_BUS_ADDRESS", f"unix:path={dbus}",
        "--bind", str(cwd), str(cwd),
        "--chdir", str(cwd),
    ]


def nix_shell_packages():
    return [
        "bubblewrap",
    ]


def override(arg):
    """Replace a function, injecting the previous version as the first argument
    so overrides can chain. Used as ``@nixulate.override`` (replaces
    ``nixulate.<name>``) or ``@nixulate.override(target)`` (replaces
    ``target`` wherever it lives)."""
    if not callable(arg):
        raise TypeError(
            f"@nixulate.override expects a function; "
            f"got {type(arg).__name__}")
    home = sys.modules.get(arg.__module__)
    if home is not None and getattr(home, arg.__name__, None) is arg:
        return lambda fn: _install(home, arg.__name__, arg, fn)
    ps_module = sys.modules[__name__]
    if not hasattr(ps_module, arg.__name__):
        raise AttributeError(
            f"nixulate has no attribute {arg.__name__!r}; for targets "
            f"outside nixulate, use the explicit form: "
            f"@nixulate.override(<module>.{arg.__name__})")
    return _install(
        ps_module, arg.__name__, getattr(ps_module, arg.__name__), arg)


def _install(module, name, target, fn):
    """Install `fn` at `module.name`, with `target` curried as first arg."""
    @functools.wraps(fn)
    def wrapper(*args, **kwargs):
        return fn(target, *args, **kwargs)
    # Rewrite so a later @nixulate.override(wrapper) routes here, not to fn's
    # original module.
    wrapper.__module__ = module.__name__
    setattr(module, name, wrapper)
    return wrapper


if __name__ == "__main__":
    main()
